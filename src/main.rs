fn main() {
    /*
      Data types
        i64 - signed integer (64-bit)
        u64 - unsigned integer (64-bit) - non-negative
        f32 - float (32-bit)
    */

    let x: i64 = 45; // i64
    let f: f32 = 7.34; // f32
    let b: bool = true; // bool
}
