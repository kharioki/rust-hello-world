fn main() {
    /*
      let = immutable variable - cannot be reassigned
      let mut = mutable variable - can be reassigned
    */
    let mut x = 45;

    println!("x is {}", x);

    x = 60;

    println!("x is now {}", x);
}
